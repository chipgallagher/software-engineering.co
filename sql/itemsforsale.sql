CREATE TABLE `db`.`itemsforsale` (
  `id` INT NOT NULL,
  `title` VARCHAR(255) NULL,
  `description` VARCHAR(255) NULL,
  `category` VARCHAR(255) NULL,
  `price` INT NULL,
  `imagepath` VARCHAR(255) NULL,
  PRIMARY KEY (`id`));
